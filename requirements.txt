flask>=3.1.3
pyrekordbox>=0.4.4
mutagen>=1.47.0
pywebview>=6.2.1
# pywebview transitive deps — pinned explicitly so pip install is deterministic
# regardless of resolver behavior, and so PyInstaller's collect_submodules('webview')
# never silently returns [] from a missing-import error at build time.
proxy-tools>=0.1.0
bottle>=0.13.4
# pythonnet provides the `clr` module pywebview uses to render the native
# window on Windows. macOS/Linux pywebview backends do not need it.
pythonnet>=3.0.0; sys_platform == "win32"
# pyobjc provides the Cocoa bridge pywebview uses to render the native
# window on macOS. Pinned explicitly so PyInstaller's collect_submodules
# and the build-time guard in release.yml can rely on these being present.
pyobjc-core>=12.1; sys_platform == "darwin"
pyobjc-framework-Cocoa>=12.1; sys_platform == "darwin"
pyobjc-framework-WebKit>=12.1; sys_platform == "darwin"
